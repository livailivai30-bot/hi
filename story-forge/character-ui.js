(()=>{const $=id=>document.getElementById(id);const render=()=>{const t=$('draft')?.value||'',box=$('characterScan');if(!box)return;const hits=StoryForge.characterCandidates(t);if(!t.trim()){box.textContent='اكتب الفصل أولًا.';return}if(!hits.length){box.innerHTML='لم أجد شخصية مؤكدة بعد. إذا كان النص يستخدم أسماء غير مميزة لغويًا، سيظهر لك خيار إضافتها يدويًا لاحقًا بدل إنشاء شخصيات خاطئة.';return}box.innerHTML=`<div class="item"><b>تم اكتشاف ${hits.length} مرشحًا:</b></div>${hits.map((x,i)=>`<div class="item" data-hit="${i}"><b>${x.name}</b> <span class="muted">${Math.round(x.confidence*100)}% · ${x.source}</span></div>`).join('')}<div class="muted" style="margin-top:8px">عند «حفظ كفصل» ستُنشأ الشخصيات الجديدة منفردة وتُربط بالفصل، بينما الموجودة أصلًا ستُحدّث ظهوراتها.</div>`};$('scanCharacters')?.addEventListener('click',render);$('draft')?.addEventListener('input',()=>{clearTimeout(window.__charScan);window.__charScan=setTimeout(render,900)});})();